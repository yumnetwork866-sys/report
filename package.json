{
  "name": "manage-team",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter backend --filter frontend dev",
    "start": "pnpm -r --parallel --filter backend --filter frontend start",
    "build": "pnpm --filter frontend build",
    "lint": "pnpm --filter backend lint && pnpm --filter frontend lint",
    "test": "pnpm --filter backend test && pnpm --filter frontend test",
    "test:coverage": "pnpm --filter backend test:coverage && pnpm --filter frontend test:coverage",
    "check": "pnpm lint && pnpm test && pnpm build",
    "backend:dev": "pnpm --filter backend dev",
    "backend:start": "pnpm --filter backend start",
    "backend:seed:demo:full": "pnpm --filter backend seed:demo:full",
    "frontend:dev": "pnpm --filter frontend dev",
    "frontend:build": "pnpm --filter frontend build"
  }
}
